import 'package:flutter/material.dart';

class AppColors {
  // Primary — Warga Blue
  static const Color primary = Color(0xFF0066FF);
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color primaryContainer = Color(0xFFE6F0FF);
  static const Color onPrimaryContainer = Color(0xFF004BB3);

  // Secondary — Warga Gold
  static const Color secondary = Color(0xFFFFD54F);
  static const Color onSecondary = Color(0xFF0066FF);
  static const Color secondaryContainer = Color(0xFFFFF2CC);
  static const Color onSecondaryContainer = Color(0xFFB38F00);

  // Surface — Clean White canvas
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceDim = Color(0xFFF5F0DB);
  static const Color surfaceBright = Color(0xFFFFFFFF);
  static const Color surfaceContainerLowest = Color(0xFFFFFFFF);
  static const Color surfaceContainerLow = Color(0xFFFFFBF0);
  static const Color surfaceContainer = Color(0xFFFFF8E1);
  static const Color surfaceContainerHigh = Color(0xFFF8F1D5);
  static const Color surfaceContainerHighest = Color(0xFFEFDEC0);

  // On-surface
  static const Color onSurface = Color(0xFF1A1A1A);
  static const Color onSurfaceVariant = Color(0xFF4A4A4A);

  // Outline
  static const Color outline = Color(0xFFFFD54F);
  static const Color outlineVariant = Color(0xFFE0C040);

  // Status
  static const Color error = Color(0xFFFF4D4D);
  static const Color onError = Color(0xFFFFFFFF);
  static const Color background = Color(0xFFF9FAFB);
  static const Color onBackground = Color(0xFF1A1A1A);

  // Survival color bands
  static const Color survivalGreen = Color(0xFF00C853);
  static const Color survivalAmber = Color(0xFFFFAB00);
  static const Color survivalRed = Color(0xFFE53935);

  // Category tags
  static const Color essential = Color(0xFF00C853);
  static const Color discretionary = Color(0xFFFF7043);
  static const Color savings = Color(0xFF0066FF);

  // Shadows
  static const Color shadowBlue = Color(0x0D0066FF); // 5% opacity
  static const Color shadowMedium = Color(0x1A000000);
}
